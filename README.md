# ugly_assembler
ugly assembler in C

# assembler

## Main file is assembler.c

## Instructions:

compile the project with:

`$ make`

compile and run:

`$ make run`

or

`$ make && ./assembler ./examples/one_command.as`

test it with:

```
$ make && ./run_tests.sh
```
